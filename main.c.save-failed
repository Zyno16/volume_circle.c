#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main()
{
    float r,volume;
    const float pi = 3.14;
    printf("Enter radius?: ");
    scanf("%f",&r);
    volume=(4*pi*pow(r,3))/3;
    printf("%.2f",volume);
    return 0;
}
